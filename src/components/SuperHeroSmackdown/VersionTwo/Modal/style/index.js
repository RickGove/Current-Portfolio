import styled from 'styled-components';

export const ModalDiv = styled.div`
	height: 100vh;
	width: 100vw;
	background: rgba(0, 0, 0, 0.9);
	position: absolute;
	top: 0;
	left: 0;

	.hidden {
		display: none;
	}
`;
